[tool.poetry]
name = "anxiety-chatbot-project"
version = "0.1.0"
description = ""
authors = ["Leon"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
Django = "^5.0.2"
daphne = "^4.1.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
